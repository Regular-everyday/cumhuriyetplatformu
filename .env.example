# Yönetim paneli şifresi (en az 12 karakter, üretimde zorunlu)
ADMIN_PASSWORD=GucluSifreniz123!

# Oturum imzalama anahtarı (en az 32 karakter, üretimde zorunlu)
# Oluşturmak için: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=buraya-en-az-32-karakterlik-rastgele-bir-anahtar-yazin

# Supabase proje URL'si
NEXT_PUBLIC_SUPABASE_URL=https://proje-refiniz.supabase.co

# Sunucu tarafı service role key
SUPABASE_SERVICE_ROLE_KEY=buraya-supabase-service-role-key-yazin

# Ekip fotograflari için public storage bucket adi
SUPABASE_TEAM_IMAGES_BUCKET=team-images
